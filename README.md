# My-LaTex-Template

## Build

Both of `Dissertation` and `Report` are compiled with XeLaTeX.

```
latexmk main.tex
```

`Beamer` is compiled with luaLaTeX.

## Clean build artifacts

```
latexmk -C
```
